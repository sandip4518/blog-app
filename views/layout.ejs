<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= typeof title !== 'undefined' ? title : 'My Blog' %></title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/styles.css" />
  <link href="https://cdn.quilljs.com/1.3.6/quill.snow.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/quill-dark.css" />
  <script src="https://cdn.quilljs.com/1.3.6/quill.js"></script>
  <script src="/js/quill-setup.js" defer></script>
</head>
<body>
  <header class="site-header">
    <div class="container">
      <a class="brand" href="/">My Blog</a>
      <nav style="display: flex; align-items: center; gap: 24px;">
        <a class="nav-link" href="/">Home</a>
        <% if (locals.user) { %>
          <a class="nav-link" href="/my-posts">My Posts</a>
          <a class="nav-link" href="/posts/new">Create Post</a>
          <form action="/logout" method="POST" style="display: inline; margin: 0;">
            <button type="submit" class="nav-link" style="background: none; border: none; cursor: pointer; font-family: inherit; font-size: inherit; padding: 0;">Logout</button>
          </form>
        <% } else { %>
          <a class="nav-link" href="/login">Login</a>
          <a class="nav-link" href="/register">Register</a>
        <% } %>
      </nav>
    </div>
  </header>
  <% if (locals.user) { %>
    <div class="container" style="padding-top: 20px; padding-bottom: 0;">
      <div style="color: var(--text-muted); font-size: 0.95rem;">
        Logged in as <strong style="color: var(--text-main);"><%= user.username %></strong>
      </div>
    </div>
  <% } %>
  <main class="container">
    <%- body %>
  </main>
  <footer class="site-footer">
    <div class="container">
      <p>&copy; <%= new Date().getFullYear() %> My Blog</p>
    </div>
  </footer>
</body>
</html>


